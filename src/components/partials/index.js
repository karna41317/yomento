import AlertStatus from './AlertStatus'
import BackgroundWrapper from './BackgroundWrapper'
import Logo from './Logo'
import GradientWrapper from './gradientWrapper'

export {
  AlertStatus, BackgroundWrapper, Logo, GradientWrapper,
}

export default {
  AlertStatus, BackgroundWrapper, Logo, GradientWrapper,
}